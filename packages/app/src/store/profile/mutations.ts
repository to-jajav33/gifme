import { MutationTree } from 'vuex';
import { ProfileStateInterface } from './state';

const mutation: MutationTree<ProfileStateInterface> = {
  someMutation (/* state: ProfileStateInterface */) {
    // your code
  }
};

export default mutation;
